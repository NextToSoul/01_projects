# PPCU TestBench - 本地覆盖配置
#
# 此文件中的配置会覆盖 config/default.yaml 中的同名键。
# 此文件已加入 .gitignore，修改不会被 git 追踪。
#
# 用法：复制需要的字段到此处，修改值即可。
# 无需复制全部字段，只写需要覆盖的部分。
#
# 示例：修改通讯地址
# comms:
#   tcp:
#     host: 192.168.1.100
#     port: 4001
